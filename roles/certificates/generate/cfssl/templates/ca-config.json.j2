{
    "signing": {
        "default": {
            "expiry": "{{ cert_info_expiry }}"
        },
        "profiles": {
            "server": {
                "expiry": "{{ cert_info_expiry }}",
                "usages": [
                    "signing",
                    "key encipherment",
                    "server auth"
                ]
            },
            "client": {
                "expiry": "{{ cert_info_expiry }}",
                "usages": [
                    "signing",
                    "key encipherment",
                    "client auth"
                ]
            },
            "peer": {
                "expiry": "{{ cert_info_expiry }}",
                "usages": [
                    "signing",
                    "key encipherment",
                    "server auth",
                    "client auth"
                ]
            }
        }
    }
}
