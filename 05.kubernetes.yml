- hosts:
  - kube_etcds
  - kube_cluster
  become: true
  gather_facts: true
  any_errors_fatal: "{{ any_errors_fatal | default(true) }}"
  roles:
  - { role: k8s-setup/ha, when: "groups['kube_masters'] | length > 1" }
  - { role: k8s-setup/install }
